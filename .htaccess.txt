RewriteEngine On
RewriteBase /

# mail.php ko bypass karo — sabse pehle
RewriteCond %{REQUEST_URI} ^/mail\.php$ [NC]
RewriteRule ^ - [L]

# Next.js static files
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]